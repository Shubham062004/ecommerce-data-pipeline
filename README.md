# E-Commerce ETL Data Pipeline & Analytics System

## Project Overview
An end-to-end Data Engineering pipeline to Extract, Transform, and Load (ETL) e-commerce transaction data. The project takes raw CSV data, cleans and processes it using Python (Pandas), loads it into a relational database, and performs SQL-based analytics to generate actionable business insights.